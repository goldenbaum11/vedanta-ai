# --- Phase 1: Foundation ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.8,<3.0
pydantic-settings>=2.5,<3.0
python-dotenv>=1.0,<2.0
httpx>=0.27,<1.0
aiosqlite>=0.20,<1.0
chromadb>=0.5,<1.0
# Lets pydantic evaluate `X | None` annotations on Python 3.9.
# Safe to keep on newer Pythons (no-op).
eval_type_backport>=0.2.0; python_version < "3.10"

# --- Phase 2: PDF ingestion (chunker.chunk_pdf imports lazily) ---
pypdf>=5.0,<6.0

# --- Phase 4: Security ---
cryptography>=43.0,<46.0
python-jose[cryptography]>=3.3,<4.0
# bcrypt 5.x dropped the __about__ shim that passlib relies on; we use
# bcrypt directly in backend/security/auth.py to dodge that mess.
bcrypt>=4.0,<6.0
slowapi>=0.1.9,<1.0

# --- Phase 6: Media (uncomment when starting Phase 6) ---
# openai-whisper>=20240930
# pytesseract>=0.3.13
# ffmpeg-python>=0.2.0
# pillow>=10.4

# --- Dev tooling ---
ruff>=0.6,<1.0
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
pytest-httpx>=0.32,<1.0
respx>=0.21,<1.0
